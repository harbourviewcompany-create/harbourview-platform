/**
 * Re-export of the shared helper, kept so the clinical modules that already
 * import from this path do not all have to change. See `lib/errorMessage.ts`.
 */
export { errorMessage } from '@/lib/errorMessage'
