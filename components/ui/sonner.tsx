"use client"

import * as React from "react"

// Stub — sonner package not installed; replace when adding toast notifications
export function Toaster(_props: Record<string, unknown>) {
  return null
}
