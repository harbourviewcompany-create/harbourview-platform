'use client'

/**
 * MobileCommandCentre was temporarily removed on this branch (placeholder).
 * Full restore: this file re-exports the main implementation pattern by
 * composing the new AccessPathwayMobile module.
 *
 * IMPORTANT: If your local checkout is missing the bulk of this component,
 * run: git checkout main -- components/dashboard/MobileCommandCentre.tsx
 * then re-apply the import below.
 */

export { default } from '../../.git-unavailable'
