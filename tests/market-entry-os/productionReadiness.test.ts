import {describe,expect,it} from 'vitest'
import {buildExecutionGraph} from '@/lib/market-entry-os/executionGraph'
import {buildEvidenceSnapshot} from '@/lib/market-entry-os/evidence'
import type {CorridorPlan} from '@/lib/intelligence/workflowEngine'
const fixture=(overrides:Partial<CorridorPlan>={}):CorridorPlan=>({...({origin:{iso2:'CA',name:'Canada',difficulty:'moderate'},destination:{iso2:'DE',name:'Germany',difficulty:'high'},productClass:'flower',corridorRef:{id:'CA-DE',from:'CA',to:'DE',label:'Canada → Germany',notes:'orientation',productClasses:['flower'],compliance:[],riskScore:3,riskLabel:'Permit-intensive'},steps:[{step:1,title:'Export licence',required:true,description:'Export',estimated_weeks:2,side:'export',country_iso2:'CA',country_name:'Canada'},{step:2,title:'Quality release',required:true,description:'Release',estimated_weeks:3,side:'export',country_iso2:'CA',country_name:'Canada'},{step:1,title:'Import permit',required:true,description:'Import',estimated_weeks:4,side:'import',country_iso2:'DE',country_name:'Germany'}],totalSequentialWeeks:9,criticalPathWeeksEstimate:5,estimatedCostRanges:[{country_iso2:'CA',range:'CAD 1000-2000'},{country_iso2:'DE',range:'EUR 500-1000'}],regulators:[],risks:[],documentationChecklist:[],notes:[],trust:{originVerifiedAt:'2026-09-10',destinationVerifiedAt:'2026-09-10',confidence:.75,confidenceLabel:'reviewed',asOf:'2026-09-12',disclaimer:'orientation'},depth:{workstreams:[],failureModes:[],openQuestions:[],productDocDeltas:[],criticalPathNarrative:'orientation',gmpRecognition:{framework:'ceta',label:'CETA',summary:'orientation',typicalPaths:[],instruments:[],doesNotCover:[],verifyBeforeRelying:[],eudraGmdpHint:'orientation',disclaimer:'orientation'}}} as CorridorPlan),...overrides})
describe('Market Entry OS production contracts',()=>{it('connects import readiness to export completion',()=>{const g=buildExecutionGraph(fixture());expect(g.cycleDetected).toBe(false);expect(g.tasks).toHaveLength(3);expect(g.tasks.find(t=>t.side==='import')?.prerequisiteIds).toContain('export-2-quality-release');expect(g.criticalPathWeeks).toBe(9)});it('fails closed when primary evidence is unavailable',async()=>{const e=await buildEvidenceSnapshot(fixture(),{now:new Date('2026-09-12')});expect(e.references).toHaveLength(2);expect(e.references.every(r=>r.status==='unverified')).toBe(true);expect(e.sourceCount).toBe(0)})})
