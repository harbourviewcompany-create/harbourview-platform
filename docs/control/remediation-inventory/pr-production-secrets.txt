# Bootstrap — replace with exact output of governance audit.
# Target: 0 files.
# Scanner detects PR-triggered workflows that reference:
#   SUPABASE_DB_URL | SUPABASE_DB_PASSWORD | SUPABASE_ACCESS_TOKEN |
#   SUPABASE_SERVICE_ROLE_KEY | VERCEL_AUTOMATION_BYPASS_SECRET
#
# Remediation: remove secrets from PR paths or move privileged jobs to trusted events only
# (push to main / workflow_dispatch / schedule). Prefer OIDC.
