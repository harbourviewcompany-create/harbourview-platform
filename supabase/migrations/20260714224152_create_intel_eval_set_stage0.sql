-- This is version 20260714224152's actual production-recorded statement history
-- entry: production applied it as the duplicate re-registration of
-- 20260714120000_create_intel_eval_set_stage0.sql's CREATE TABLE, roughly six
-- hours after that earlier version.
--
-- Kept as a no-op deliberately (see 20260714120000_create_intel_eval_set_stage0.sql's
-- header, restored 2026-08-05 in commit 3d4041a6 "fix(db): restore intel eval
-- set creator at its replay-correct version"): a from-scratch replay needs
-- public.intel_eval_set to exist by the time 20260714120100
-- (expose_intel_eval_set_via_api_schema) and 20260714120200
-- (add_intel_eval_structural_crosscheck) run, which is earlier than this
-- version. The table's real CREATE TABLE therefore lives at 20260714120000
-- instead of here, and this version staying a no-op is what makes that work --
-- a plain CREATE TABLE here would fail with "relation already exists" against
-- a replay that already created it at 20260714120000.
--
-- 2026-08-31: found reverted back to a verbatim `create table` (the actual
-- historical production statement for this version) by a later, unrelated
-- re-run of scripts/reconstruct-stub-migrations.mjs, which mechanically
-- reconstructs every remaining SELECT-1 placeholder from
-- supabase_migrations.schema_migrations.statements and has no awareness that
-- this particular placeholder had been deliberately left as a no-op for a
-- documented, replay-specific reason. Restored to the no-op here. See
-- docs/control/RECONSTRUCTED_MIGRATION_IDEMPOTENCY_AUDIT_2026-08-31.md for
-- the broader pattern this points at: any other file with a "restore ... for
-- replay" / "restore ... at replay-correct version" history is a candidate
-- for the same silent regression on the next reconstruction re-run, not just
-- for a fresh idempotency bug.
--
-- Production is unaffected either way: this version is already recorded
-- remotely, so `supabase db push` never re-applies it.
select 1;
