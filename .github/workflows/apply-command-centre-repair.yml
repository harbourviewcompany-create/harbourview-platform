name: Apply Command Centre Repair

on:
  push:
    branches:
      - build/harbourview-production-command-platform
    paths:
      - .github/workflows/apply-command-centre-repair.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: apply-command-centre-repair
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout exact branch
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: build/harbourview-production-command-platform
          fetch-depth: 1
          persist-credentials: true

      - name: Verify and extract reviewed non-workflow repair payload
        shell: bash
        run: |
          set -euo pipefail
          cp .github/workflows/production-security-hardening.yml /tmp/production-security-hardening.yml
          cat .github/repair-payload.part-* > /tmp/hv-repair-payload.b64
          base64 --decode /tmp/hv-repair-payload.b64 > /tmp/hv-repair-payload.tar.gz
          echo 'ec2e9f50321fea752b5decc9e88e3eca437adf42a410e7172fa1320a6257f124  /tmp/hv-repair-payload.tar.gz' | sha256sum --check --strict
          python - <<'PY'
          from pathlib import Path
          import tarfile

          archive_path = Path('/tmp/hv-repair-payload.tar.gz')
          with tarfile.open(archive_path, 'r:gz') as archive:
              for member in archive.getmembers():
                  target = Path(member.name)
                  if target.is_absolute() or '..' in target.parts:
                      raise SystemExit(f'unsafe archive member: {member.name}')
              archive.extractall('.')
          PY

          cp /tmp/production-security-hardening.yml .github/workflows/production-security-hardening.yml
          rm -f .github/repair-payload.part-*

          test "$(git hash-object supabase/migrations/20260302000000_intelligence_globe_v1.sql)" = '1003ae45a05c32ecd9ae005719be3c67fef3a2ac'
          grep -Fq "('public','marketplace_public_listings_v1','public')" supabase/migrations/20260804190000_production_security_hardening.sql
          grep -Fq 'page=marketplace&section=marketplace&marketView=cannabis' tests/e2e/mobile-command-centre-v2.spec.ts
          git diff --check

      - name: Commit focused non-workflow repair
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'harbourview-release-bot'
          git config user.email 'release-bot@users.noreply.github.com'
          git add -A
          git reset -- .github/workflows/apply-command-centre-repair.yml .github/workflows/production-security-hardening.yml
          git commit -m 'fix(release): repair command centre verification gates'
          git push origin HEAD:build/harbourview-production-command-platform
