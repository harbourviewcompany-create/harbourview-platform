name: Temporary Export Replacement Head

on:
  pull_request:
    paths:
      - '.github/workflows/tmp-export-replacement-head.yml'

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.base.sha }}
      - name: Verify exact base
        run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.base.sha }}"
      - name: Package exact replacement base
        run: tar --exclude=.git -czf /tmp/harbourview-replacement-f574.tar.gz .
      - uses: actions/upload-artifact@v4
        with:
          name: harbourview-replacement-f574
          path: /tmp/harbourview-replacement-f574.tar.gz
          retention-days: 1
          if-no-files-found: error
