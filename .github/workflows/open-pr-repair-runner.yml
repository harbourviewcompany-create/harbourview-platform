name: Open PR Repair Runner

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/open-pr-repair-runner.yml'
      - 'scripts/open-pr-repair/**'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  actions: read

jobs:
  rebase-pr1224:
    if: github.event_name == 'pull_request' && github.event.pull_request.number == 1241
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: agent/open-pr-repair-runner
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Rebuild reviewed PR 1224 on current main
        run: bash scripts/open-pr-repair/rebase-pr1224-current-main.sh
