# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js build output
.next/
out/

# Production
build/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local
.env

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Local transient test artifacts
tmp-test-*.bin

# OpenNext
.open-next

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example

!.env.example
.claude/worktrees/
.sentryclirc

# Supabase CLI scratch written by `supabase start`. Contains a start-secrets
# directory; must never be committed. The candidate workflow does `git add -A`,
# so without this it would commit local dev secrets into the repository.
supabase/.temp/

# Playwright local output — generated, never committed.
# A 517KB report was swept in by `git add -A` on 0687fb10 and pushed the PR
# diff past the review bots' size limits, costing the PR its review.
playwright-report/
test-results/

# Local Claude Code overrides -- never commit.
# Carries a production Supabase write grant scoped to this machine.
.claude/settings.local.json
